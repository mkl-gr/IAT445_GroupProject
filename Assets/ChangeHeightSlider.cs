using UnityEngine;

public class ChangeHeightSlider : MonoBehaviour
{
    public float scaleFactor = 0.1f; // How much to change scale by
    public float minScale = 0.5f; // Minimum allowed scale
    public float maxScale = 2.0f; // Maximum allowed scale

    void Update()
    {
    }

    void ScalePlayer(float amount)
    {
    }
}